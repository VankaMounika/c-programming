#include <stdio.h>

int main() {
	int arr[]={2,3,8,5,6,13,10,17,59};
	int len=sizeof(arr)/sizeof(int);
	int f1,f2,i,j,n,count=0;
	for(i=0;i<len;i++)
	{
		scanf("%d",&f1);
		for(j=2;j<n/2;j++)
		{
			scanf("%d",&f2);
			if(f1==f2)
			count++;
		}
	}
		if(count==0)
		{
		    printf("unique");
		}
		    else
		    {
		    printf("duplicate");
		}
    return 0;
}


output:

unique
